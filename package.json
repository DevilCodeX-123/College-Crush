{
  "name": "college-crush-root",
  "version": "1.0.0",
  "description": "Root package for ClgCrush",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev-frontend": "cd frontend && npm run dev",
    "dev-backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev-frontend\" \"npm run dev-backend\"",
    "build": "cd frontend && npm install && npm run build && node -e \"const fs=require('fs'); fs.rmSync('../public', {recursive:true, force:true}); fs.renameSync('dist', '../public');\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}